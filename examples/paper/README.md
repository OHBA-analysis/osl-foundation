Directories:
- `train_models`: Contains scripts for training models.
- `generate_data`: Contains scripts for generating data.
- `analysis`: Contains scripts for analysing results.

Users should run the scripts in the `train_models` directory first to train the models, then use the `generate_data` scripts to generate data for analysis, and finally use the `analysis` scripts to analyse the results.