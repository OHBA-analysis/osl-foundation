model_config:
  decoder_parameters:
    dropout: 0.2
    feed_forward_activation: gelu
    feed_forward_dim: 90
    model_dim: 90
    n_heads: 4
    n_patches_in: [100, 20, 20, 10]
    patch_length_in: [1, 4, 4, 8]
    unpatched_length_in: [0, 0, 0, 0]
    l_unpatched_b: [None, None, None, None]
    l_patched_b: [8, None, None, None]
    do_chan_attention: [False, True, True, True]
    do_cross_attention: [False, False, False, False]
    chan_attention_mask: [None, None, None, None]
    channel_dropout_rate: 0.5
    full_channel_attention_dropout: 0.1
    chan_attention_channel_dropout_rate: 0.1
    chan_attention_chandim: 4
  input_parameters:
    channel_embedding_dim: 50
    embedding_dim: 50
    extra_labels:
    - label_dim: 50
      n_classes: 612
      name: session_id
    pos_embedding_dim: 50
    token_embedding_dim: 50
  loss_parameters:
    loss_sequence_length: 15
  n_channels: 52
  name: ephys_gpt
  sequence_length: 100
  tokenizer_path: /Users/woolrich/dev/results/osl-foundation/camcan_spring23/tokenizer
training_config:
  batch_size: 8
  n_epochs: 1
  optimizer:
    learning_rate: 1.0e-05
  multi_gpu: False
  checkpoint:
    save_dir: /Users/woolrich/dev/results/osl-foundation/camcan_spring23/generator_cTrue_l80
